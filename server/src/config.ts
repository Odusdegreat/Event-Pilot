import { z } from "zod";
const environment = z.object({ PORT: z.coerce.number().int().positive().default(4000), CORS_ORIGIN: z.string().min(1).default("http://localhost:3000").transform((value) => value.split(",").map((origin) => origin.trim())), OPENROUTER_API_KEY: z.string().min(1), OPENROUTER_MODEL: z.string().default("openai/gpt-4o-mini"), MEMWAL_PRIVATE_KEY: z.string().min(1), MEMWAL_ACCOUNT_ID: z.string().min(1), MEMWAL_SERVER_URL: z.string().url().default("https://relayer.memory.walrus.xyz"), NIGERIA_EVENTS_URL: z.string().url().default("https://techlinkup.xyz/api/events"), EVENT_FEED_REFRESH_HOURS: z.coerce.number().int().positive().default(6) });
export const config = environment.parse(process.env);
